export default function ConnectCouple() {
    return (
        <section id={"connect"}>
            <p>연결 코드를 입력해주세요.</p>
            <input type={"text"} />
            <button>연결코드 생성</button>
        </section>
    );
}